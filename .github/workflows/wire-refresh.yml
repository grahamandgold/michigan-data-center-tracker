name: Hourly wire refresh (Grok agent)

on:
  schedule:
    - cron: "15 * * * *"   # every hour at :15 UTC
  workflow_dispatch: {}     # manual run button

permissions:
  contents: write

concurrency:
  group: wire-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run Grok wire agent
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          XAI_MODEL: ${{ vars.XAI_MODEL || '' }}
        run: python scripts/wire_agent.py

      - name: Verify headline accuracy against sources
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          XAI_MODEL: ${{ vars.XAI_MODEL || '' }}
        run: python scripts/headline_check.py

      - name: Second-model newsworthiness judge (Claude grades Grok's picks)
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          JUDGE_THRESHOLD: ${{ vars.JUDGE_THRESHOLD || '6' }}
        run: python scripts/news_judge.py

      - name: Commit if changed
        run: |
          if git status --porcelain live-data.json live-data-pending.json desk-decisions.json | grep -q .; then
            git config user.name "grok-wire-agent"
            git config user.email "wire-agent@users.noreply.github.com"
            git add live-data.json live-data-pending.json desk-decisions.json
            git commit -m "Wire: file to desk queue $(date -u +'%Y-%m-%d %H:%M UTC')"
            git pull --rebase origin main
            git push
          else
            echo "No changes — skipping commit."
          fi
